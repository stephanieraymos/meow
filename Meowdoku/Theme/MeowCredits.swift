import Foundation

/// Attribution for third-party assets. CC-BY requires crediting the creator in
/// the shipping app.
enum MeowCredits {
    // TODO: paste the exact text from the model's Sketchfab "Copy Credits"
    // button (creator name + license). Placeholder until confirmed.
    static let assets = """
    "Bored Cat" 3D model via Sketchfab, licensed under Creative Commons \
    Attribution (CC BY). Credit the original creator here.
    """
}
